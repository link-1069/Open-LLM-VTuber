# Open-LLM-VTuber

Open-LLM-VTuber 将实时对话与外部数字人视频流组合为桌面数字人体验。

## Language

**活动流 ID**:
活动数字人推流的 `av_stream_id`，由活动流发现服务返回，并用于生成对应的 WHEP 拉流地址。
_Avoid_: 最新 ID、数字人 ID

**连接测试**:
检查由 WHEP 地址推导出的 SRS HTTP API 是否返回成功状态；它只代表 SRS 服务可达，不代表 WHEP 流能够播放或已有视频画面。
_Avoid_: WHEP 播放测试、画面就绪检测

**自动接入**:
应用在启动、故障恢复或用户主动重新检测时先清除已保存的 WHEP 地址，再发现当前活动流 ID 并执行连接测试；第一次完整检测轮次同时满足两个条件后保存新地址并启动数字人，真实视频首帧就绪后才进入主展示窗口，只有成功进入展示流程的地址才能保留，任何失败都不会终止后续完整轮次。
_Avoid_: 首次运行设置、旧配置直连

**检测轮次**:
一次实际启动的自动接入尝试：先请求并发现一个有效的活动流 ID，再测试由该 ID 生成的 SRS 地址；因前一轮未结束而跳过的检测节拍不构成轮次，任一步失败都会使整个轮次失败。
_Avoid_: 跨轮成功、沿用旧 ID

**自动恢复**:
数字人画面的真实 WHEP 拉流失败或中断后，应用返回自动接入并重新发现活动流 ID，而不是持续使用已保存的旧地址；已有候选流仍继续启动，画面就绪后可直接完成接入。`disconnected` 持续三秒才算中断，`failed`、`closed` 或媒体轨道结束则立即算中断。
_Avoid_: 旧地址重连、手动恢复

**活动流冷却**:
真实 WHEP 拉流失败的活动流 ID 在五秒内不能再次触发连接测试和数字人展示；冷却期间仍继续发现活动流，新的 ID 可以立即接入。
_Avoid_: 永久屏蔽、立即重试失败 ID

**活动流替换**:
自动接入和主展示期间都持续发现活动流，并以发现服务最新返回的有效活动流 ID 为准；不同的新 ID 通过自己的连接测试后在后台启动，只有画面就绪且新地址保存成功后才取代当前流；候选保存失败时保留候选并继续重试，发现结果回到当前显示 ID 时立即取消其他候选，空结果或失败不会取消当前流或候选流。
_Avoid_: 等待旧流超时、先关闭当前画面、同时展示多个流

### 桌面呈现

**自动接入窗口**:
用于执行并展示自动接入的纯状态桌面窗口，实时呈现活动流发现、连接测试、失败原因和重试状态；不提供手动输入或操作，自动保存成功后进入主展示窗口。
_Avoid_: 连接设置窗口、手动设置窗口、配置页面、后台静默检测

**接入进度**:
自动接入窗口呈现的当前检测轮次、活动流 ID 状态与当前 ID、连接测试状态、失败原因、下一轮重试提示，以及 WHEP 建连和画面就绪等待的剩余时间。
_Avoid_: 单行状态、完整检测日志

**画面就绪**:
数字人视频已解码出有效帧，并且透明数字人舞台已经至少成功呈现一次该画面。
_Avoid_: WHEP 已连接、视频正在播放

**重新检测连接**:
用户从主展示窗口主动结束当前数字人展示并启动一次新的自动接入。
_Avoid_: 设置投流地址、手动配置连接

**主展示窗口**:
真实视频首帧就绪后用于呈现数字人视频流和字幕的透明、可拖拽桌面窗口。
_Avoid_: 保存连接后显示的页面、主页面

**窗口控制面板**:
主展示窗口内用于调整并保存该窗口位置和尺寸的浮动界面。
_Avoid_: 设置页面、独立控制窗口

**控制热区**:
主展示窗口右上角用于唤出窗口控制面板的不可见交互区域。
_Avoid_: 透明按钮、可见按钮
